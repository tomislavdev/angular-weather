import { FloorNumberPipe } from './floor-number.pipe';

describe('FloorNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FloorNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
